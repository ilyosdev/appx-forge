# Managed by Ansible -- do not edit manually
# T-06-12: This file is mode 0600 owned by root. Contains sensitive URLs.

# Control plane URL (typically a Tailscale internal address)
FORGE_CONTROL_URL={{ forge_control_url }}

# Agent HTTP listen port for file push endpoint
FORGE_AGENT_LISTEN_PORT={{ forge_agent_listen_port | default(8090) }}

# Docker image for sandbox containers
FORGE_SANDBOX_IMAGE={{ forge_sandbox_image }}

# Base directory for sandbox code bind mounts
FORGE_CODE_DIR={{ forge_code_dir | default('/var/lib/forge/sandboxes') }}
