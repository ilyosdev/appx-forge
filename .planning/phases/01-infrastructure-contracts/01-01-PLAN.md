---
phase: 01-infrastructure-contracts
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - go.work
  - shared-go/go.mod
  - shared-go/models/sandbox.go
  - shared-go/models/node.go
  - shared-go/models/command.go
  - shared-go/models/route.go
  - shared-go/models/sandbox_test.go
  - shared-go/auth/hmac.go
  - shared-go/auth/hmac_test.go
  - control/go.mod
autonomous: true
requirements: [CNTR-06]

must_haves:
  truths:
    - "Sandbox state machine defines all 8 states and 10 events with a map-based transition table"
    - "Every non-terminal state can reach StateDestroyed (no orphaned states)"
    - "EventDestroyRequest is accepted from every active state"
    - "Invalid transitions return false from NextState()"
    - "HMAC-SHA256 signed URLs can be generated and verified with constant-time comparison"
    - "Go workspace resolves shared-go and control modules"
  artifacts:
    - path: "go.work"
      provides: "Go workspace root"
      contains: "shared-go"
    - path: "shared-go/models/sandbox.go"
      provides: "SandboxState, SandboxEvent types, ValidTransitions map, NextState function"
      exports: ["SandboxState", "SandboxEvent", "ValidTransitions", "NextState"]
    - path: "shared-go/models/sandbox_test.go"
      provides: "Property tests for state machine"
      min_lines: 60
    - path: "shared-go/auth/hmac.go"
      provides: "SignURL and VerifyURL functions"
      exports: ["SignURL", "VerifyURL"]
    - path: "shared-go/auth/hmac_test.go"
      provides: "HMAC sign/verify tests"
      min_lines: 30
  key_links:
    - from: "shared-go/models/sandbox.go"
      to: "control/ (future)"
      via: "Go workspace module reference"
      pattern: "models\\.SandboxState"
---

<objective>
Scaffold the Go workspace, define the shared model types (sandbox states, events, node, command, route), implement the sandbox state machine transition table with TDD property tests, and implement HMAC-SHA256 signed URL utilities with TDD.

Purpose: The state machine transition table is the single most critical contract in the entire system -- every subsequent phase (agent, control plane, scheduler) depends on correct state transitions. Getting this wrong causes cascading failures. TDD ensures the transition table is exhaustively verified before any downstream code is written. HMAC utilities are needed for signed URLs in the file-push protocol.

Output: Working Go workspace with shared-go module containing tested state machine and HMAC utilities, plus control module scaffolding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-contracts/01-RESEARCH.md
@STARTER_PLAN.md
@CLAUDE_CODE_KICKOFF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Go workspace and shared-go module with model types</name>
  <files>go.work, shared-go/go.mod, shared-go/models/sandbox.go, shared-go/models/node.go, shared-go/models/command.go, shared-go/models/route.go, control/go.mod</files>
  <read_first>
    - STARTER_PLAN.md (repo structure, model definitions)
    - CLAUDE_CODE_KICKOFF.md (coding conventions)
    - control-api.openapi.yaml (schema definitions for Sandbox, Node, AgentCommand, Route)
  </read_first>
  <action>
1. Create `go.work` at repo root with Go 1.25 directive and `use` directives for `./shared-go` and `./control`.

2. Create `shared-go/go.mod` with module path `github.com/appx/forge/shared-go`, Go 1.25 directive. Run `go get github.com/google/uuid@v1.6.0`.

3. Create `control/go.mod` with module path `github.com/appx/forge/control`, Go 1.25 directive. Add `require github.com/appx/forge/shared-go v0.0.0` with a `replace github.com/appx/forge/shared-go => ../shared-go` directive. Run `go get` for: `github.com/jackc/pgx/v5@latest`, `github.com/go-chi/chi/v5@v5.2.5`, `github.com/kelseyhightower/envconfig@v1.4.0`, `github.com/google/uuid@v1.6.0`, `github.com/testcontainers/testcontainers-go@v0.42.0`, `github.com/testcontainers/testcontainers-go/modules/postgres@v0.42.0`.

4. Create `shared-go/models/sandbox.go` with package `models` containing:
   - `SandboxState` string type with constants: `StatePending = "pending"`, `StateStarting = "starting"`, `StateRunning = "running"`, `StateRestarting = "restarting"`, `StateStopped = "stopped"`, `StateDestroying = "destroying"`, `StateDestroyed = "destroyed"`, `StateFailed = "failed"`
   - `SandboxEvent` string type with constants: `EventScheduled = "scheduled"`, `EventStarted = "started"`, `EventContainerExited = "container_exited"`, `EventIdleTimeout = "idle_timeout"`, `EventDestroyRequest = "destroy_requested"`, `EventDestroyed = "destroyed"`, `EventRestartAttempt = "restart_attempt"`, `EventNodeFailed = "node_failed"`, `EventStartFailed = "start_failed"`
   - `ValidTransitions` map as defined in RESEARCH.md (the full transition table -- see Pattern: State Machine Transition Table)
   - `NextState(current SandboxState, event SandboxEvent) (SandboxState, bool)` function
   - `AllStates() []SandboxState` helper returning all states
   - `AllEvents() []SandboxEvent` helper returning all events
   - `IsTerminal(state SandboxState) bool` returning true for StateDestroyed

5. Create `shared-go/models/node.go` with package `models` containing:
   - `NodeStatus` string type with constants: `NodeHealthy = "healthy"`, `NodeUnhealthy = "unhealthy"`, `NodeDraining = "draining"`, `NodeRemoved = "removed"`

6. Create `shared-go/models/command.go` with package `models` containing:
   - `CommandType` string type with constants: `CmdStartSandbox = "start_sandbox"`, `CmdStopSandbox = "stop_sandbox"`, `CmdRestartSandbox = "restart_sandbox"`, `CmdGetLogs = "get_logs"`, `CmdPrune = "prune"`
   - `CommandStatus` string type with constants: `CmdPending = "pending"`, `CmdDispatched = "dispatched"`, `CmdCompleted = "completed"`, `CmdFailed = "failed"`

7. Create `shared-go/models/route.go` with package `models` containing:
   - No struct definitions needed (sqlc will generate those), just type aliases or constants if any.
   - Add a comment: `// Route models are generated by sqlc from Postgres schema. This file holds shared route-related constants.`

8. Verify with `cd /Users/mac/Documents/my-products/appx-forge && go work sync && cd shared-go && go vet ./... && cd ../control && go vet ./...`
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/my-products/appx-forge && go work sync && cd shared-go && go vet ./... && echo "shared-go OK" && cd ../control && go vet ./... && echo "control OK"</automated>
  </verify>
  <acceptance_criteria>
    - go.work contains `use ./shared-go` and `use ./control`
    - shared-go/go.mod contains `module github.com/appx/forge/shared-go`
    - shared-go/models/sandbox.go contains `StatePending SandboxState = "pending"`
    - shared-go/models/sandbox.go contains `func NextState(`
    - shared-go/models/sandbox.go contains `ValidTransitions`
    - shared-go/models/sandbox.go contains 8 SandboxState constants
    - shared-go/models/sandbox.go contains 9 SandboxEvent constants
    - shared-go/models/node.go contains `NodeHealthy NodeStatus = "healthy"`
    - shared-go/models/command.go contains `CmdStartSandbox CommandType = "start_sandbox"`
    - control/go.mod contains `replace github.com/appx/forge/shared-go => ../shared-go`
    - `go vet ./...` passes in both shared-go and control directories
  </acceptance_criteria>
  <done>Go workspace compiles, shared-go has all model type definitions matching the OpenAPI spec states/enums, control module scaffold exists with dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD -- State machine property tests and HMAC signed URL tests</name>
  <files>shared-go/models/sandbox_test.go, shared-go/auth/hmac.go, shared-go/auth/hmac_test.go</files>
  <read_first>
    - shared-go/models/sandbox.go (just created -- need current state)
    - STARTER_PLAN.md (state machine diagram)
    - 01-RESEARCH.md (TDD test examples, HMAC pattern)
  </read_first>
  <behavior>
    State Machine Tests:
    - TestAllStatesHaveTransitions: every non-terminal state has at least one outgoing transition
    - TestDestroyedIsTerminal: StateDestroyed has zero outgoing transitions
    - TestEveryStateCanReachDestroyed: BFS/DFS from any state reaches StateDestroyed
    - TestDestroyRequestAlwaysAccepted: EventDestroyRequest works from pending, starting, running, restarting, stopped, failed
    - TestInvalidTransitionsRejected: (destroyed,started)=invalid, (pending,started)=invalid, (running,scheduled)=invalid, (starting,idle_timeout)=invalid, (destroyed,scheduled)=invalid
    - TestValidTransitionsAccepted: (pending,scheduled)=starting, (starting,started)=running, (running,container_exited)=restarting, (running,destroy_requested)=destroying, (destroying,destroyed)=destroyed, (running,idle_timeout)=stopped, (running,node_failed)=pending, (failed,restart_attempt)=starting

    HMAC Tests:
    - TestSignAndVerify: sign URL with key, verify returns true
    - TestVerifyRejectsTamperedURL: modify URL after signing, verify returns false
    - TestVerifyRejectsExpiredURL: sign with 0s expiry, verify returns false
    - TestVerifyRejectsWrongKey: sign with key A, verify with key B returns false
    - TestSignedURLContainsExpiry: signed URL has `expires` query param
    - TestSignedURLContainsSignature: signed URL has `sig` query param
  </behavior>
  <action>
RED phase:
1. Create `shared-go/models/sandbox_test.go` with all 8 test functions listed above (exact test code from RESEARCH.md Pattern: TDD Test: State Machine Property Tests, plus TestValidTransitionsAccepted table-driven test).
2. Run `cd /Users/mac/Documents/my-products/appx-forge/shared-go && go test ./models/` -- tests MUST pass because implementation exists from Task 1.
   Note: If all tests pass immediately, this is expected since Task 1 created the implementation. This is acceptable for foundational types where the type definitions ARE the implementation. Commit as `test(01-01): add state machine property tests`.

3. Create `shared-go/auth/hmac_test.go` with all 6 HMAC test functions.
   `SignURL(rawURL string, key []byte, expiry time.Duration) (string, error)` -- appends `expires` (Unix timestamp) and `sig` (hex-encoded HMAC-SHA256 of URL+expires) as query params.
   `VerifyURL(signedURL string, key []byte) (string, error)` -- extracts `sig` and `expires`, checks expiry, recomputes HMAC with `hmac.Equal()`, returns original URL without sig/expires params on success.
4. Run tests -- MUST fail (no implementation yet). Commit: `test(01-01): add failing HMAC signed URL tests`.

GREEN phase:
5. Create `shared-go/auth/hmac.go` implementing `SignURL` and `VerifyURL`:
   - `SignURL`: parse URL, add `expires` param (now + expiry as Unix timestamp string), compute HMAC-SHA256 over `path?query` (without sig), hex-encode, add as `sig` param.
   - `VerifyURL`: parse URL, extract `sig` and `expires` params, check `expires` >= now (return error "URL expired" if not), remove sig from query, recompute HMAC-SHA256 over `path?remaining_query`, compare with `hmac.Equal()`, return stripped URL on success.
   - Use `crypto/hmac`, `crypto/sha256`, `encoding/hex`, `net/url`, `strconv`, `time` from stdlib only.
6. Run `go test ./auth/` -- MUST pass. Commit: `feat(01-01): implement HMAC signed URL generation and verification`.

REFACTOR (if needed):
7. Review both packages, clean up if obvious improvements. Run all tests: `go test ./...`. Commit if changes made.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/my-products/appx-forge/shared-go && go test -v ./...</automated>
  </verify>
  <acceptance_criteria>
    - shared-go/models/sandbox_test.go contains `TestAllStatesHaveTransitions`
    - shared-go/models/sandbox_test.go contains `TestDestroyedIsTerminal`
    - shared-go/models/sandbox_test.go contains `TestEveryStateCanReachDestroyed`
    - shared-go/models/sandbox_test.go contains `TestDestroyRequestAlwaysAccepted`
    - shared-go/models/sandbox_test.go contains `TestInvalidTransitionsRejected`
    - shared-go/models/sandbox_test.go contains `TestValidTransitionsAccepted`
    - shared-go/auth/hmac.go contains `func SignURL(`
    - shared-go/auth/hmac.go contains `func VerifyURL(`
    - shared-go/auth/hmac.go contains `hmac.Equal(`
    - shared-go/auth/hmac_test.go contains `TestSignAndVerify`
    - shared-go/auth/hmac_test.go contains `TestVerifyRejectsExpiredURL`
    - shared-go/auth/hmac_test.go contains `TestVerifyRejectsWrongKey`
    - `go test ./...` exits 0 in shared-go directory
  </acceptance_criteria>
  <done>All 14 tests pass (8 state machine property tests + 6 HMAC tests), TDD commits present in git log</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signed URL generation | HMAC secret must not leak; URLs must expire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | shared-go/auth/hmac.go | mitigate | Use hmac.Equal() for constant-time comparison (prevents timing attacks) |
| T-01-02 | Information Disclosure | shared-go/auth/hmac.go | mitigate | Strip sig and expires params from returned URL in VerifyURL to prevent forwarding signed URLs |
| T-01-03 | Elevation of Privilege | shared-go/models/sandbox.go | mitigate | State machine rejects invalid transitions via ValidTransitions map -- no bypass path |
</threat_model>

<verification>
- `cd /Users/mac/Documents/my-products/appx-forge/shared-go && go test -v ./...` -- all tests pass
- `cd /Users/mac/Documents/my-products/appx-forge && go work sync` -- workspace resolves
- `cd /Users/mac/Documents/my-products/appx-forge/control && go vet ./...` -- control module compiles
- `grep -r "StatePending" shared-go/models/sandbox.go` -- state constants present
- `grep -r "hmac.Equal" shared-go/auth/hmac.go` -- constant-time comparison used
</verification>

<success_criteria>
- Go workspace with shared-go and control modules compiles cleanly
- 8 state machine property tests pass covering reachability, terminality, destroy-from-any-state, and invalid transition rejection
- 6 HMAC tests pass covering sign/verify, tampering, expiry, wrong key
- TDD commit pattern visible in git log (test commits before feat commits for HMAC)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-contracts/01-01-SUMMARY.md`
</output>
