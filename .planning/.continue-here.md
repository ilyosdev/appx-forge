# AppX Forge — Production State

**Updated:** 2026-04-17
**Branch:** gsd/phase-07-multi-node-failover (pushed to origin)
**Status:** PRODUCTION WORKING — sandboxes create, run HTTPS, wildcard cert

---

## What Works Right Now

```
Server 1: 89.117.60.147  (CapRover host — Forge control plane)
  ✓ forge-control  systemd, port 8090, healthy, migrations v5
  ✓ Postgres       Docker, port 5433, db: forge
  ✓ Bug fixes deployed: HandleAck race, NULL payload, heartbeat recovery

Server 2: 161.97.135.158  (Agent + Caddy + sandbox containers)
  ✓ forge-agent    systemd, registered as forge-node-1
  ✓ Caddy v2.11.2  custom build with Cloudflare DNS plugin
  ✓ Wildcard cert  *.myappx.live via DNS-01 (Let's Encrypt, no rate limits)
  ✓ appx/sandbox:v1  image (production)
  ✓ appx/sandbox:v2  image built (Dockerfile fixes, not yet default)

Verified E2E:
  ✓ https://live.myappx.live → HTTPS 200, Metro packager-status:running
  ✓ container_id + host_port persisted in DB (Bug 1 fix verified)
  ✓ Node auto-recovers to healthy after heartbeats resume
  ✓ Drift detector syncs Caddy routes from Postgres

appx-api (text2design/backend):
  ✓ Local .env points at production forge-control
  ✓ CapRover env: RAILOVER vars removed, FORGE vars added
  ✗ Backend CODE not yet deployed (still has old Railover code in production)
```

---

## Caddy Configuration (Server 2)

```
Config: /etc/caddy/caddy.json
Binary: /usr/bin/caddy (custom build with dns.providers.cloudflare)
Cert:   Wildcard *.myappx.live via DNS-01 + Cloudflare API
Token:  (stored in /etc/caddy/caddy.json on Server 2)

Admin API: 0.0.0.0:2019 (firewalled to Server 1 + localhost)
HTTPS: :443 with tls_connection_policies
HTTP:  :80 → 301 redirect to HTTPS

Routes are managed by forge-control drift detector (syncs every 60s).
Routes are also added on HandleAck when sandbox transitions to running.
```

---

## Bugs Fixed This Session

1. HandleAck CAS race condition — tolerates state already advanced
2. container_id/host_port persistence via UpdateSandboxRuntime query
3. NULL event payload — safePayload() helper across all lifecycle files
4. Dockerfile UID 1000 conflict + WORKDIR + CI=1 (image v2 built)
5. Heartbeat auto-recovery — UpdateNodeHeartbeat sets status='healthy'
6. Caddy drift detector — connected to Server 2 Caddy admin API

---

## What's Next (Priority Order)

### 1. Deploy Backend Code to Production
```
The backend has 44 uncommitted files (Railover→Forge migration).
CapRover env vars are already updated (FORGE vars set, RAILOVER removed).
Steps:
  1. Commit the 44 backend files on the appropriate branch
  2. Deploy to CapRover (password: captain42)
  3. Verify ForgeService connects to production forge-control
  4. Monitor first real sandbox creations from the web UI
```

### 2. Test File Push E2E
```
Once backend is deployed:
  - Call ForgeService.pushCode() via appx-api
  - Verify 307 redirect to agent file push endpoint
  - Verify HMAC signed URL validates on agent side
  - Verify Metro detects file change via inotify, rebundles
  - This is the core AI-generation → live preview loop
```

### 3. Switch Default Image to v2
```
appx/sandbox:v2 is built on Server 2 with Dockerfile fixes.
Update FORGE_SANDBOX_IMAGE=appx/sandbox:v2 in:
  - backend/.env (local)
  - CapRover appx-api env vars
  - /opt/forge/.env on Server 1 (forge-control default)
```

### 4. Clean Up Ghost Sandboxes
```
Several destroyed/stuck sandboxes in Postgres:
  - second-app, test-prod-app (stuck on dead contabo-vds-2 node)
  - test-bugfix, e2e-v2, e2e-v3, demo, preview (destroyed)
  - Delete via: DELETE FROM sandboxes WHERE state = 'destroyed';
  - Delete dead node: DELETE FROM nodes WHERE hostname = 'contabo-vds-2';
```

---

## Quick Ops Commands

```bash
# Health check
curl http://89.117.60.147:8090/v1/healthz

# List nodes
curl -H "Authorization: Bearer c1e032ab65907431dd4fa699a92851c9475caa3320452d9e179fb13df5fce7c6" \
  http://89.117.60.147:8090/v1/nodes

# List sandboxes
curl -H "Authorization: Bearer c1e032ab65907431dd4fa699a92851c9475caa3320452d9e179fb13df5fce7c6" \
  http://89.117.60.147:8090/v1/sandboxes

# Caddy routes
sshpass -p '0WzUnyorap72k$Dk' ssh root@161.97.135.158 \
  'curl -s http://localhost:2019/config/apps/http/servers/srv0/routes | python3 -m json.tool'

# Tail forge-control logs
sshpass -p 'm54caxryDcWnRBVR' ssh root@89.117.60.147 'journalctl -u forge-control -f'

# Tail agent logs
sshpass -p '0WzUnyorap72k$Dk' ssh root@161.97.135.158 'journalctl -u forge-agent -f'

# Tail Caddy logs
sshpass -p '0WzUnyorap72k$Dk' ssh root@161.97.135.158 'journalctl -u caddy -f'

# Rebuild + deploy forge-control
cd /Users/mac/Documents/my-products/appx-forge && \
  GOOS=linux GOARCH=amd64 go build -o bin/forge-control-linux ./control/cmd/forge-control/ && \
  sshpass -p 'm54caxryDcWnRBVR' ssh root@89.117.60.147 'systemctl stop forge-control' && \
  sshpass -p 'm54caxryDcWnRBVR' scp bin/forge-control-linux root@89.117.60.147:/usr/local/bin/forge-control && \
  sshpass -p 'm54caxryDcWnRBVR' ssh root@89.117.60.147 'chmod +x /usr/local/bin/forge-control && systemctl start forge-control'
```

---

## Server Credentials

```
Server 1: root@89.117.60.147  pass: m54caxryDcWnRBVR
Server 2: root@161.97.135.158 pass: 0WzUnyorap72k$Dk
CapRover: captain42
Forge API: c1e032ab65907431dd4fa699a92851c9475caa3320452d9e179fb13df5fce7c6
Forge HMAC: b8c73b011bd3e2adcfe138526f54da12037f629ab27f678d6bbd040bf97c44b3
Cloudflare API: (stored in /etc/caddy/caddy.json on Server 2)
Postgres: forge:forge_prod_2026@localhost:5433/forge
```
